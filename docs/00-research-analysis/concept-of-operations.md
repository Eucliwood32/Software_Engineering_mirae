# Concept of Operations (ConOps)
## QCE — 부탁해 꼬마선장 (Quantitative Contribution Evaluator)

| 항목 | 내용 |
| --- | --- |
| 문서 버전 | v1.5 |
| 작성일 | 2026-06-01 |
| 준수 표준 | ISO/IEC/IEEE 29148-2018 §9.3 (ConOps) |
| 관련 문서 | Problem Statement v1.2 (선행), Requirements Record v1.5 (병행, 요구사항 정본) |
| 작성 주체 | QCE 개발팀 (20222047 조원희 · 20247142 이대한 · 20221985 김휘중) |
| 검토 주기 | FR/NFR 변경 시 즉시 갱신, 그 외 학기당 1회 |

---

## 1. 문서 개요

### 1.1 목적
본 문서는 QCE(Quantitative Contribution Evaluator) 시스템이 **누구를 위해, 어떤 환경에서, 어떻게 운영되는지**를 이해관계자 관점에서 정의한다. Problem Statement가 "왜 필요한가"를 정의한다면, ConOps는 "어떻게 사용될 것인가"를 다루고, Requirements Record가 그로부터 "무엇을 만족해야 하는가"를 도출한다.

### 1.2 적용 범위
- 시스템: QCE 데스크톱 애플리케이션 (Python 3.10+ Standalone)
- 운영 주체: 팀 프로젝트 조장 (단일 사용자)
- 운영 시점: 학기 중 중간/기말 평가, 또는 임의의 정량 평가 시점
- 운영 환경: 조장 개인 PC, Air-Gapped 단독 실행

### 1.3 본 문서가 다루지 않는 것
- 구체적 함수 시그니처, 클래스 다이어그램 → `02-design-planning/` 참조
- 테스트 케이스 정의 → `03-verification-validation/` 참조
- 빌드/패키징 절차 → `06-implementation/build-and-package.md` 참조 (아이콘 적용 여부 등)

### 1.4 참조 문서
- Problem Statement v1.2
- Requirements Record v1.5 (`01-requirements/00-Requirement-records.md`) — 요구사항 정본
- `05-decisions/ADR-0001-MVC.md`, `ADR-0002-PyQt6.md`, `ADR-0003-IsolationForest.md`

### 1.5 용어
RR의 용어 정의를 우선 따르며, 본 문서에서 추가로 사용하는 용어는 다음과 같다.

- **운영 세션(Operational Session)**: 조장이 QCE를 실행한 시점부터 종료할 때까지의 단일 사용 단위. NFR-2.3에 따라 세션 종료 시 민감 데이터는 휘발 파기된다. 단, 사용자 설정(가중치 프리셋 등)은 이 정책의 예외다.
- **평가 사이클(Evaluation Cycle)**: 입력 수집 → 분석 → 리포트 검토(필요 시 결과 화면에서 동일인 계정 병합 → 재집계) → 판단까지의 1회 평가 흐름. 신원 매핑은 분석 후 결과 화면에서 수행한다(FR-1.3/FR-5.7).
- **어뷰징(Abusing)**: 자동 생성 코드 일괄 커밋, 비정상적 대량 푸시 등 정량 지표를 왜곡할 의도/결과의 행위.
- **이상 신호(Anomaly Signal)**: 시스템이 통계·규칙으로 식별하여 조장에게 표시하는 검토 권유 신호. 판정·결론이 아니다.

---

## 2. 현재 상황 (As-Is)

### 2.1 현재의 평가 방식
QCE 도입 이전, 조장이 팀 기여도를 평가하는 방식은 일반적으로 다음 중 하나에 해당한다.

1. **주관 평가**: 조장이 "체감상" 누가 얼마나 기여했는지 판단. 객관적 근거 부재.
2. **수기 집계**: GitHub 커밋 수를 수동으로 세고, 카톡 대화량을 눈으로 훑고, 발표자료 작성자를 기억에 의존해 정리.
3. **단순 자가 보고**: 팀원이 직접 기여도를 % 형태로 보고하고 조장이 조정.

### 2.2 As-Is의 한계
- **L1. 통합 불가**: 코드 기여, 문서 작성, 메신저 소통이 각각 다른 도구·플랫폼에 흩어져 있어 단일 지표로 통합 비교가 불가능하다.
- **L2. 어뷰징 무방비**: AI 생성 코드를 통째로 커밋하거나, 마감 직전 대량 푸시로 활동량을 부풀리는 행위를 자동으로 탐지할 수단이 없다.
- **L3. 신원 분산**: 한 팀원이 GitHub `dh-lee`, 카톡 `이대한`, 다른 플랫폼 `daehan.lee` 등 플랫폼마다 다른 식별자를 가져 통합 추적이 어렵다.
- **L4. 메신저 잡음**: "ㅇㅇ", "ㅋㅋ", 이모티콘 등이 메시지 수에 포함되어, 실제 협업 소통량과 형식적 응답을 구분하지 못한다.
- **L5. 시각화 부재**: 데이터를 모아도 비교 가능한 형태로 시각화하는 도구가 없어 의사결정 비용이 크다. 나아가, 최종 점수만 도출되고 그 기반이 되는 요소(코드 양, 문서 텍스트 수 등)가 가시화되지 않아 평가 결과에 대한 불신을 초래할 수 있다.
- **L6. 시간 비용**: 위 작업을 수기로 처리하면 1회 평가에 수 시간이 소요되며, 결과의 일관성도 보장되지 않는다.

### 2.3 As-Is의 이해관계자 통증점
- **조장**: 평가 시간 부담, 판단 근거의 객관성 부족, 팀원과의 형평성 시비 위험.
- **팀원**: 기여도가 정성 평가에 좌우되어 노력이 가시화되지 않는다는 불만 가능성.
- **교수/조교**: 조장의 보고를 검증할 객관적 자료가 부족.

---

## 3. 변경의 필요성과 본질 (Justification)

### 3.1 왜 지금 변경하는가
- 팀 프로젝트 규모가 커지고 협업 채널이 다변화(Git, 메신저, 문서)되면서, 단일 채널 관찰로는 기여도를 측정할 수 없다.
- 생성형 AI 도구 보급으로 코드/문서 어뷰징 가능성이 구조적으로 증가했다.
- 동시에, 외부 LLM API에 팀 데이터를 업로드하는 것은 프라이버시·보안 측면에서 부적절하다.

### 3.2 변경의 본질
QCE는 **"수기 → 자동 통합 + 어뷰징 신호 + 시각화"**로의 전환이며, 다음 4대 목표(O-1~O-4)로 환원된다.

- O-1 통합분석: `.pptx/.docx/.hwpx` + 메신저 로그 + Git을 단일 파이프라인으로 결합
- O-2 어뷰징 신호: 자동 생성 코드·비정상 커밋의 통계적 탐지·표시 (판정이 아닌 신호)
- O-3 시각화: 종합 지표·지표별 분포·산점도를 통한 항목 간 상관 분포를 막대·레이더·산점도 3종 리포트로 제공하되, 산출의 투명성을 위해 원시 데이터(코드 양, 글자 수 등)를 명시적으로 노출
- O-4 완전 로컬: 외부 AI/클라우드 없이 조장 PC에서 단독 작동

### 3.3 명시적 비목표 (재확인)
- 실시간 협업 모니터링 아님 (배치성 정량 평가 도구)
- LLM 기반 의미 분석 아님 (규칙 기반 NLP)
- 팀원 PC에 에이전트를 설치하지 않음
- 외부 서버로 데이터를 전송하지 않음
- 시스템이 평가 결론·등급·징계 판단을 자동으로 내리지 않음 (판정 금지 원칙, §8.1 P5 참조)

---

## 4. 제안 시스템 (To-Be) 개요

### 4.1 시스템 한 줄 요약
> 조장의 PC에서 단독 실행되며, 문서·코드·메신저 3종 입력을 한 번에 받아 팀원별 정량 기여 지표(원시 데이터 포함)와 이상 신호를 시각화 리포트로 제공하는 데스크톱 도구. 시스템은 신호를 제시하고, 판단은 조장에게 귀속된다.

### 4.2 운영 모드
QCE는 단일 운영 모드를 가진다(다중 모드 없음).

- **분석 세션 모드**: 조장이 입력 데이터를 모아 **제출 화면**에서 적재·가중치 설정 → **로딩 화면**(분석) → **결과 화면**(리포트 조회, 필요 시 동일인 계정 병합 → 재집계)을 일괄 수행한다(3-스크린, FR-5.4). 결과 화면에서 **[새 분석]**을 요청하면 이전 세션의 데이터 입력 상태는 모두 초기화되어 즉시 새로운 평가 사이클을 시작할 수 있다. 세션 종료 시 NFR-2.3에 따라 민감 데이터는 메모리·임시 파일에서 모두 파기된다(가중치 프리셋 등 사용자 설정은 예외).

### 4.3 To-Be의 가치 명제

| As-Is 한계 | To-Be 해결 수단 | 관련 FR |
| --- | --- | --- |
| L1 통합 불가 | 3종 입력(OOXML/HWPX·Git·카카오톡) 단일 파이프라인 + 정규화 특성 매트릭스 | FR-1, FR-2, FR-3, FR-4.1 |
| L2 어뷰징 무방비 | EW-01(≥1000줄 단일 커밋), EW-02(빈도×3 폭주), Z-Score 하위 이상치 신호 | FR-4.2, FR-4.2b, FR-4.2d |
| L3 신원 분산 | Global Alias Mapping UI | FR-1.3 |
| L4 메신저 잡음 | 시스템 내장 자동 불용어 분류 + 형태소 분석 + 유효 글자수 산출 | FR-3.3 |
| L5 시각화 부재 | 막대·레이더·산점도 3종 시각화 및 세부 구성요소(원시 데이터) 노출 명문화 | FR-5.1, FR-5.1a, FR-5.1b, FR-5.1c, FR-5.1d |
| L6 시간 비용 | 전체 파이프라인 ≤300초 (NFR-3) | NFR-3 |

---

## 5. 운영 환경

### 5.1 물리 환경
- 조장의 일반 데스크톱 또는 노트북 1대 (특수 하드웨어 요구 없음)
- 인터넷 연결은 빌드/설치 단계 이후에는 불필요 (Air-Gapped 동작 가능, NFR-2.2)

### 5.2 시스템 환경
- OS: Windows 10/11
- 런타임: Python 3.10+ 또는 PyInstaller 패키지 실행 (전용 앱 아이콘 적용 완료)
- NLP 의존: 순수 Python·CPython C 확장 라이브러리만 사용 (kiwipiepy, soynlp 등). JRE 의존 형태소 분석기 사용 금지.
- Git CLI: 로컬 저장소 분석 시 필요 — `git log` 텍스트 파일만 입력하는 경우 미필요

### 5.3 자원 한계
- RAM ≤ 2GB 사용
- 처리 시간 상한: Git 500커밋 ≤60s, 카톡 10MB ≤120s, 전체 파이프라인 ≤300s

### 5.4 데이터 환경
- 입력 데이터는 조장이 사전에 수집하여 로컬 디스크에 둔다(외부 자동 fetch 없음).
- 입력 형식: `.pptx/.docx/.hwpx`(문서), `.txt`(카카오톡 export), 로컬 Git 저장소 또는 `git log` 텍스트.
- 산출 데이터: 분석 결과(메모리 내 DataFrame, UI 화면, 캐시 파일의 익명화 통계)는 세션 종료 시 또는 NFR-2.3 정책에 따라 휘발/제한 저장된다. **사용자 설정(가중치 프리셋 등)은 이 정책의 명시적 예외로서 로컬에 영속 저장된다.**

---

## 6. 이해관계자 및 사용자 특성

### 6.1 1차 사용자 — 조장
- **인원**: 1명 (단일 사용자 전용, RBAC 미지원)
- **기술 친숙도**: 컴퓨터공학/정보 계열 학부생 수준 가정. CLI는 가능하나 GUI 친화 환경을 선호.
- **사용 빈도**: 학기당 2~4회 (중간·기말 평가 시점, 필요 시 중간 점검)
- **주요 관심사**: 평가의 객관성·재현성, 어뷰징 식별, 결과 시각화의 명확성, 기여도 산출 근거(원시 데이터)의 투명성, 결과 해석의 자율성(시스템이 결론을 대신 내리지 않을 것).

### 6.2 2차 이해관계자
| 이해관계자 | 관심 | QCE와의 관계 |
| --- | --- | --- |
| 팀원 (평가 대상, 2~10인) | 본인 기여가 공정하게 측정되는가 | 직접 사용은 하지 않으나, 입력 데이터의 생성자 |
| 교수/조교 | 조장 보고의 신뢰성 | 조장이 산출한 리포트를 수신할 수 있음 |
| 개발팀(본 프로젝트 3인) | 유지보수성, RTM 추적성 | NFR 및 RTM 정책 책임 |

### 6.3 사용자 특성 가정
- 팀 규모는 2~10인 범위로 한정한다.
- 조장은 입력 데이터를 본인이 수집·배치할 책임이 있으며, QCE는 데이터 자동 수집을 수행하지 않는다.

### 6.4 사용자가 가지지 않는 권한
- 외부 서버 전송 기능 없음
- 팀원 PC에 에이전트를 설치하는 기능 없음
- 결과를 클라우드에 백업하는 기능 없음
- 불용어 사전 편집 기능 없음 (시스템이 자동 결정, FR-3.3)

---

## 7. 운영 시나리오

### 7.1 시나리오 A — 학기말 종합 평가 (메인 시나리오)

**상황.** 15주차 기말, 조장은 팀 4인의 학기 전체 기여도를 정량 평가해 교수에게 제출해야 한다. 조 구성:
- 본인(조장)
- B 팀원 (백엔드 담당)
- C 팀원 (프론트엔드 담당)
- D 팀원 (문서·발표 담당)

**선행 조건.**
- 팀 GitHub 저장소가 조장 PC에 로컬 클론되어 있다.
- 카카오톡 단톡방 대화가 학기 시작일부터 종강일까지 `.txt`로 export되어 있다.
- 발표 자료(`.pptx`) 3개, 기획서/보고서(`.docx`) 4개, 한글 문서(`.hwpx`) 1개가 한 폴더에 모여 있다.

**흐름.**

1. **실행 (UC-1 진입).** 조장이 전용 아이콘이 적용된 `QCE.exe`를 더블클릭한다. **제출 화면**이 뜨고, 큼직한 로고와 프로그램 설명, 중앙에 멀티포맷 Drag&Drop 영역, 가중치 설정 패널, [분석 시작] 버튼이 표시된다. 이때, 차트가 렌더링될 데이터가 없는 상태에서는 미리 "분석할 데이터가 없습니다."라는 안내 문구가 표시되어 직관성을 돕는다. (캐시가 있으면 즉시 결과 화면으로 진입, NFR-2.4.)

2. **문서 적재.** 조장이 8개 문서 파일(.pptx/.docx/.hwpx 혼재)을 한 번에 Drag&Drop으로 던진다. UI 파일 목록에 "8/8 적재 완료" 문구와 함께 **입력된 문서들의 구체적인 파일명이 명시적으로 표시**된다. (파일이 없으면 '없음'으로 표시된다.) 내부적으로 각 파일의 작성자·수정자·유효 글자수가 추출된다.

3. **Git 적재.** 조장이 메뉴에서 "Git 저장소 선택"을 눌러 로컬 클론 경로를 지정한다. **화면에 선택된 Git 저장소의 폴더 이름이 표시된다.** 시스템이 커밋별 작성자·날짜·추가/삭제 행을 수집한다. 500커밋 가정 시 처리 시간은 60초 이내.

4. **메신저 적재.** 조장이 카톡 export `.txt`를 Drag&Drop한다. **적재된 카톡 텍스트 파일명이 화면에 명시적으로 표시**된다. 시스템이 발화자·시각·메시지 구조로 정규화하며, 이 과정에서 수집된 발화자(Alias) 식별자는 이후 결과 화면의 매핑 목록에 누락 없이 100% 표시됨이 보장된다. 카톡 데이터만 단독으로 적재하고 분석을 실행해도 파이프라인이 중단되지 않고 정상적으로 결과 화면을 반환한다. 시스템 내장 자동 불용어 분류가 적용되며 사용자에게 사전 편집 UI는 제공되지 않는다.

5. **가중치 설정.** 조장이 제출 화면의 가중치 영역(상단에 **"작업 종류 별 반영 비율"**이라는 명시적 설명 문구가 존재함)에서 비중을 조절한다. **가중치 슬라이더를 조작하면, 총합이 1이 되도록 나머지 항목이 실시간으로 자동 연동(재분배)되며, 현재 적용된 가중치가 명확한 숫자(예: 0.60)로 실시간 텍스트로 표기**되어 조장의 빠르고 정확한 의사결정을 돕는다. 

6. **분석 실행 (UC-5).** 조장이 "분석 시작" 버튼을 누르면 **로딩 화면**으로 전환되어 진행률이 표시된다. 시스템은:
   - 팀원×지표 매트릭스를 구성하고 정규화
   - Capping·로그 스케일 적용
   - EW-01·EW-02·Z-Score 하위 이상치 신호를 식별
   - 데이터 소스 결측 시 가중치 동적 재조정
   - 완료되면 결과 화면으로 전환된다(오류 시 제출 화면 복귀).

7. **리포트 조회 (UC-6) 및 원시 데이터 검토.** **결과 화면**이 표시된다. 조장은 차트를 확인하며, 시스템은 점수 산출의 근거인 **원시 데이터(Git의 추가된 코드 라인 수, 문서의 실질적인 텍스트 양/문자 횟수, 메신저 유효 텍스트 양 등 정량적 요소)**를 툴팁과 시각자료에 노출하여 최종 점수의 구성요소를 투명하게 제공한다.
   - **막대 차트** — 팀원별 종합 기여 지표 및 세부 구성 점수 툴팁.
   - **레이더 차트** — Git·문서·메신저 3축.
   - **산점도** — 입력 데이터 개수에 따라 동적 표시 (1개: 산점도 없음 안내, 2개: Y축 첫 번째/X축 두 번째 입력 데이터, 3개: Y축 첫 번째/X축 두 번째/점 색(두 색상 그라데이션 보간) 세 번째 입력 데이터).

   **계정 병합 (UC-2, FR-5.7).** 조장이 서로 다른 식별자를 1인으로 묶는 매핑을 수행한다. 
   - **방어 로직 1 (매핑 미선택 방지):** 매핑(병합)할 항목을 하나도 선택하지 않은 상태라면, 확인(OK) 버튼이 비활성화되어 무의미한 빈 화면 재집계나 분석 오류를 원천 차단한다.
   - **방어 로직 2 (안전한 취소):** 매핑 창에서 작업 중 취소(Cancel) 버튼을 누를 경우, 전체 매핑 기능이 멈추거나 화면이 증발하는 오류 없이 해당 매핑 모달 창만 깔끔하게 닫히고 기존 화면 상태를 유지한다.

8. **신호 확인 (UC-7).** 우측 신호 영역에 이상 신호(어뷰징 의심, 비정상 빈도 등)가 표시된다.

9. **판단 및 종료 (초기화).** 조장은 리포트를 export하고 결과를 검토한다. 작업을 마친 후 화면의 **[새 분석]** 버튼을 누르면, 시스템은 제출 첫 화면으로 돌아가면서 **이전에 적재했던 모든 데이터(문서, 메신저, 깃 경로)와 UI 상태를 완전히 초기화(리셋)**하여 깨끗한 상태로 새로운 분석을 시작할 수 있게 한다.

**예상 소요 시간.** 입력 적재 ~3분, 매핑 ~2분, 분석 ~5분, 리포트 검토 ~10분. 총 약 20분 이내.

---

### 7.2 시나리오 B — 어뷰징 의심 케이스 대응

**상황.** 시나리오 A의 8단계에서 B 팀원의 단일 커밋 1,847줄 신호를 받았다. 조장은 이를 검증해야 한다.

**흐름.**

1. **신호 항목 클릭.** 조장이 신호 카드를 클릭하면 해당 커밋의 상세 정보가 표시된다.
2. **소스 확인.** 조장이 로컬 클론에서 해당 커밋을 직접 열어 확인한다.
3. **케이스 B-1 판단.** 조장이 정상(보일러플레이트 코드 등)으로 판단한 경우, 시스템의 종합 기여 지표는 시스템 신호와 무관하게 산출된 원래 값을 그대로 유지한다.
4. **케이스 B-2 처리.** 조장이 어뷰징을 의심한 경우, 신호를 유지한 채로 리포트를 export하고 별도의 팀원 면담 등을 통해 최종 판단을 내린다. (시스템은 어뷰징 확정을 내리지 않는다.)

---

### 7.3 시나리오 C — 신원 매핑 갱신

**상황.** 학기 중반, 한 팀원이 GitHub 계정을 새로 만들어 커밋하기 시작했다. 기존 alias 매핑이 더 이상 유효하지 않다.

**흐름.**

1. **새 분석 세션 시작.** 조장이 QCE를 실행하거나 결과 화면에서 **[새 분석]**을 눌러 기존 데이터가 **완전히 초기화된 제출 화면**으로 진입한다. 
2. **입력 적재 및 분석.** 새 계정이 등장한 데이터를 적재하고 분석을 실행한다.
3. **결과 화면에서 분리 발견.** 1차 분석은 매핑 없이 산출되므로 새 계정이 별개 인물로 표시된다.
4. **계정 병합.** 조장이 결과 화면 병합 컨트롤에서 새 계정과 기존 계정을 1인으로 안전하게 병합한다. (취소 시 기존 결과 화면 안전 복귀 보장)
5. **재집계.** 시스템이 병합 매핑으로 원시 지표를 합산해 전체 재정규화하고 시각 자료를 다시 그린다.

---

## 8. 운영 정책 및 제약

### 8.1 사용자가 반드시 인지해야 할 정책

- **P1. 데이터 휘발.** QCE 종료 시 분석 결과·임시 파일이 메모리에서 즉시 파기된다(NFR-2.3). 리포트가 필요하면 종료 전 화면 캡처·export를 수행한다.
- **P2. 외부 전송 0.** QCE는 단 1바이트도 외부망으로 전송하지 않는다(NFR-2.2). 입력 데이터는 조장 PC를 벗어나지 않는다.
- **P3. AI 미사용.** QCE는 외부 LLM/AI API를 호출하지 않는다. 모든 판정은 결정론적 규칙·통계 모델 기반이다.
- **P4. 단일 사용자.** 다중 사용자 동시 사용은 지원하지 않는다.
- **P5. 판단의 책임.** QCE는 이상 신호를 **식별·제시**하지만, **판정하지 않는다**. 시스템은 판단의 재료가 될 **각 점수의 원시 데이터(구성 요소)를 투명하게 노출**할 뿐, 최종 평가는 오직 조장의 책임이다.
- **P6. 불용어 자동 처리.** 의미 희박 표현의 분류는 시스템이 자동 수행하며, 사용자 편집은 제공되지 않는다.

### 8.2 운영 제약

| ID | 제약 | 운영 의미 |
| --- | --- | --- |
| C-1 | 로컬 CPU/RAM만 사용 | 클라우드 가속 옵션 없음 |
| C-2 | 외부 LLM 금지 | 의미 분석은 규칙 기반에 한정 |
| C-3 | 타 팀원 PC 설치 금지 | 자동 데이터 수집 불가 — 조장이 수동 적재 |
| C-4 | MVC 엄격 준수 | 운영 영향 없음(개발 제약) |
| C-5 | 명시 Python 스택 | 운영 영향 없음 |
| C-6 | 단일 실행 파일 | 설치/배포가 단순 — USB 전달 가능 |

### 8.3 가정의 운영 영향
- Git 로그를 텍스트 파일로만 받을 수도 있으므로, 팀원이 클론 권한이 없는 저장소에서도 사용 가능하다.
- 입력 문서(.pptx/.docx/.hwpx)가 손상된 경우 해당 파일은 skip되며 경고가 표시된다(FR-1.1).
- 팀원 수 1인 또는 11인 이상은 명시적으로 미지원이다.

---

## 9. 영향 분석 (Summary of Impacts)
(기존 유지)

---

## 10. 대안 및 트레이드오프
(기존 유지)

---

## 11. 부록: 시나리오-유스케이스 매핑 요약
(기존 유지)

---

## 12. 문서 변경 이력

| 버전 | 일자 | 변경 내용 | 작성자 |
| --- | --- | --- | --- |
| v1.0 | 2026-05-27 | 최초 작성 | QCE 개발팀 |
| v1.1 | 2026-05-28 | 상위 문서 관계 정정, 시각화 종수 반영 등 | QCE 개발팀 |
| v1.2 | 2026-05-28 | 상위 문서 참조 갱신, .hwpx 포맷 추가, 자동 불용어 분류 반영 등 | QCE 개발팀 |
| v1.3 | 2026-05-31 | 3-스크린 구조 및 결과 화면 계정 병합(Post-hoc Merge) 흐름 반영 | QCE 개발팀 |
| **v1.4** | **2026-06-01** | **사용자 피드백(UX/UI 버그 개선) 반영: (1) §7.1.1 차트 placeholder 텍스트('데이터 없음') 및 앱 아이콘 적용 명시, (2) §7.1.2~4 화면상 파일명/저장소명 명시적 노출, (3) §7.1.4 카카오톡 식별자 파싱 보장 및 단독 분석 보장, (4) §7.1.5 가중치 바 명칭 추가, 실시간 연동 및 숫자 표기, (5) §7.1.7 기여 지표 원시 데이터 노출 투명화(개선1), (6) §7.1.7 매핑 UI 캔슬 증발 버그 및 미선택 시 OK 방어 로직 추가, (7) §7.1.9, §7.3.1 [새 분석] 클릭 시 이전 데이터 초기화 적용.** | QCE 개발팀 |